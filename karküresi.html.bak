<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mutlu Yıllar Sevgilim</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: radial-gradient(circle, #1a2a6c, #b21f1f, #222);
            font-family: 'Arial', sans-serif;
            overflow: hidden;
        }

        .title {
            color: white;
            text-align: center;
            font-size: 1.4rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
            padding: 0 20px;
            font-weight: 300;
        }

        .globe-container {
            position: relative;
            width: 320px;
            height: 320px;
        }

        .globe {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 8px solid rgba(255, 255, 255, 0.8);
            background: black;
            position: relative;
            overflow: hidden;
            box-shadow: 0 0 60px rgba(255,255,255,0.2), inset 0 0 40px rgba(255,255,255,0.4);
            z-index: 2;
        }

        .globe-video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.8;
        }

        .base {
            width: 240px;
            height: 60px;
            background: linear-gradient(#4b240b, #2d1a0a);
            margin: -20px auto 0;
            border-radius: 10px 10px 50% 50%;
            box-shadow: 0 10px 25px rgba(0,0,0,0.6);
            z-index: 1;
        }

        /* Kar ve Şekil Stil */
        .snowflake {
            position: absolute;
            pointer-events: none;
            z-index: 3;
            user-select: none;
            line-height: 1;
        }

        .snow-button {
            margin-top: 40px;
            padding: 15px 35px;
            font-size: 1.2rem;
            cursor: pointer;
            background: #fff;
            color: #b21f1f;
            border: none;
            border-radius: 50px;
            font-weight: bold;
            box-shadow: 0 5px 20px rgba(0,0,0,0.4);
            transition: all 0.3s ease;
        }

        .snow-button:hover { transform: scale(1.05); }

        @keyframes fall {
            0% { transform: translateY(-20px) rotate(0deg); opacity: 0; }
            10% { opacity: 1; }
            100% { transform: translateY(330px) rotate(360deg); opacity: 0.3; }
        }
    </style>
</head>
<body>

    <audio id="bgMusic">
        <source src="DILERIM_KI_SARKI_LINKI.mp3" type="audio/mpeg">
    </audio>

    <div class="title">
        Seninle bir yıl değil, bir ömür... <br> ❤️❤️❤️ Mutlu yıllar sevgilim ❤️❤️❤️
    </div>

    <div class="globe-container">
        <div class="globe" id="snowGlobe">
            <video class="globe-video" id="myVideo" loop muted playsinline>
                <source src="bizvideo.mp4" type="video/mp4">
            </video>
        </div>
        <div class="base"></div>
    </div>

    <button class="snow-button" id="playBtn" onclick="startMagic()">Kar Yağsın ❄️</button>

    <script>
        function startMagic() {
            const music = document.getElementById('bgMusic');
            const video = document.getElementById('myVideo');
            const btn = document.getElementById('playBtn');
            const globe = document.getElementById('snowGlobe');

            // Müziği ve Videoyu Başlat
            music.play();
            video.play();
            
            btn.innerHTML = "İyi ki varsın... ✨";
            btn.style.opacity = "0.6";

            // Kar ve Şekil Yağmuru
            setInterval(() => {
                const flake = document.createElement('div');
                flake.className = 'snowflake';
                
                const shapes = ['❄', '❅', '❆', '❤', '✨'];
                const randomShape = shapes[Math.floor(Math.random() * shapes.length)];
                
                flake.innerHTML = randomShape;
                flake.style.left = Math.random() * 100 + '%';
                flake.style.fontSize = Math.random() * 15 + 10 + 'px';
                flake.style.color = randomShape === '❤' ? '#ff4d4d' : '#fff';
                flake.style.top = '-20px';
                
                const duration = Math.random() * 3 + 3;
                flake.style.animation = `fall ${duration}s linear forwards`;
                
                globe.appendChild(flake);

                setTimeout(() => { flake.remove(); }, duration * 1000);
            }, 150);
        }
    </script>
</body>
</html>
